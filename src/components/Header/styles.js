import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  header: {
    display: "flex",
    flexDirection: "row",
    height: 75,
    backgroundColor: "#942E96",
    width: "100%",
    alignItems: "center",
    justifyContent: "center",
    gap: 15,
    color: " #FFF0F0",
  },
}));

export default useStyles;
